{
  "iteration": 1,
  "frozen": "2026-08-22",
  "task": "cadec-pharmacovigilance-triage",
  "gold_rule": "strict = the predicted SNOMED CT code is in the gold code set for that mention; CONCEPT_LESS is correct only against CONCEPT_LESS gold",
  "gold_rule_note": "CADEC gold is not always a single code: 252 mentions are post-coordinated (A + B) and 3 are disjunctions (A or B). Scoring 'in the gold set' gives credit for either half of a post-coordinated pair. 2.8% of mentions are affected; report them separately.",
  "unit_of_evaluation": "one mention record",
  "seed": 42,
  "corpus": {
    "name": "CADEC",
    "version": "CADEC v2 (corpus); DAP collection v3 (csiro:10948, 10.4225/08/570FB102BDAD2)",
    "licence": "CSIRO Data Licence - non-commercial, NON-TRANSFERABLE, not redistributable",
    "cadec_root": "data/cadec/data/cadec",
    "splits_dir": "data/splits",
    "n_docs_total": 1250,
    "n_mentions_total": 9111,
    "split_unit": "document",
    "n_dev_docs": 40,
    "n_test_docs": 60,
    "stratified_by": "drug family (lipitor | diclofenac)",
    "checksums": "docs/cadec-checksums.txt"
  },
  "vocabulary": {
    "snomed_release": "SnomedCT_Release_AU1000036_20260731",
    "snomed_release_dir": "data/SnomedCT_Release_AU1000036_20260731",
    "snomed_db": "ladder/cache/snomed.sqlite",
    "meddra_release": "CADEC-derived code list (NOT a MedDRA release)",
    "meddra_note": "The only MedDRA table available is the code list CADEC ships with the corpus. Measured 2026-08-22: 666 codes, all 666 appear in CADEC's gold annotations, 0 do not - it is the answer key's code inventory (~3% of MedDRA PT), not a vocabulary. Removing its occurrences/posts/example_mentions columns removes the evidence of derivation, not the derivation. So rungs.1.meddra_check defaults to 'flag': the verdict is recorded and counted in rung 1's comparison and is not a rejection reason. Set 'reject' to include it as one, or point meddra_csv at a subscription release and the caveat goes away.",
    "meddra_csv": "data/meddra_codes.csv",
    "snomed_backend": "local-rf2",
    "snomed_backend_note": "local-rf2 | ols4. Not interchangeable: an OLS4-backed exists() reports 23.9% of CADEC gold as nonexistent codes (7.5% retired, 16.4% AU-extension = 100% of drug mentions). A rung 1 rejection rate is not comparable across backends. See schemas/vocabulary.py and `python -m ladder.vocab_crosscheck`.",
    "meddra_mode": "reference",
    "meddra_mode_note": "reference | answer_space. reference = the list only cross-checks a code produced by other means; retrieval from it is refused. answer_space = the task IS closed-set assignment over this list, which is a DIFFERENT and much easier task and must be declared in the method section.",
    "snomed_release_note": "Snapshot from the ALL distribution. Cache built 2026-08-22; 928 active / 115 inactive / 3 absent over CADEC gold. A prior build measured 927 active — one concept, release-dependent."
  },
  "model": {
    "extractor": "ollama/granite4:micro-h",
    "judge": "ollama/qwen2.5:7b",
    "temperature": 0,
    "note": "Judge must be a different model family from the extractor. Resolved in ladder/llm.py:for_rung — rungs never pick a model."
  },
  "rung_order": [
    0,
    1,
    2,
    3,
    4,
    5,
    6
  ],
  "rung_order_note": "Rung ID equals execution position. Renumbered 2026-08-23 from [0,1,3,5,4,2,6]; old->new is 3->2, 5->3, 2->5, others unchanged. Every measurement in docs/decisions.md dated before then uses the OLD ids — the mapping table is in that file. Order is still read from here rather than assumed, so a different order stays testable.",
  "rung0_mode": "recall",
  "rung0_mode_note": "recall | search. Plan v16 §2.1 recommends 'recall' for iteration 1 — the model emits a code from memory, no tools — because that is what 'bare LLM' means and it is what makes rung 1's rejection rate meaningful. The scaffolding's template defaulted to 'search'; the A/B between them is the iteration-2 experiment, listed under `ablations`. Whichever is used, it changes what every number above it means, so it lives here.",
  "ablations": [
    {
      "name": "rung0 search vs recall",
      "rung0_mode": "search",
      "scope": "R0+R1 only",
      "why": "POST-HOC ONLY — the search runs after generation and the model never sees it, so this measures prompt wording plus a coincidence rate, NOT tool access. A real tool loop (model calls search mid-generation) is untested and is the experiment this ablation is often mistaken for."
    },
    {
      "name": "rung order",
      "rung_order": [
        0,
        1,
        2,
        3,
        5,
        4,
        6
      ],
      "why": "abstention early. Turns 'abstention should run last' from an assertion into a measurement."
    },
    {
      "name": "vocabulary backend",
      "snomed_backend": "ols4",
      "scope": "R0+R1 only",
      "why": "the two backends disagree on 23.9% of gold; this prices that difference in rung-1 terms."
    }
  ],
  "rungs": {
    "0": {
      "rung0_offsets": "search",
      "rung0_offsets_note": "Discard the model offsets and locate span_text by string search. Measured: offsets are 19% correct at best, while the quote itself is verbatim 77% of the time.",
      "rung0_step": null,
      "rung0_step_note": "null | S0 | S1 | S2 — the rung 0 prompt-engineering study, added 2026-08-23. null keeps the original A/B mode path. SCOPE IS IDENTICAL IN ALL THREE STEPS: same mentions, same record keys, same scorer. The only thing that varies is where the CODE comes from — S0 recalls label and code from the model, S1 recalls the label and resolves the code from the keyword table, S2 picks the label from a shortlist retrieved for the mention. Set per run with `--rung0-step`, which writes the choice back into this manifest so the copy saved beside the results says which step produced them. Call counts differ (S0/S1 one call, S2 two) and are reported as cost, not hidden. S3 — a pick from one fixed printed list — was DROPPED 2026-08-24: its MedDRA list was the answer key's own inventory, the best ontology-native alternative (SNOMED's Clinical manifestation refset, 743 codes) caps at 48.7% of gold, the real keyword table is 227,554 rows and cannot be printed, and a list retrieved per mention is S2 by another name.",
      "rung0_shortlist_k": 20,
      "rung0_shortlist_note": "How many candidates S2 shows. Retrieval is token overlap over SNOMED descriptions, filtered to clinical findings. Measured 2026-08-23: exact matching the patient's own words returns nothing for 57.1% of gold reaction mentions, and where it returns something the gold code is absent 15% of the time — 'gas' returns |Gaseous substance|. Hence both the shortlist and the findings filter.",
      "keyword_table": "data/keywords.csv",
      "keyword_table_note": "Where rung 0 turns a NAME into a code. Built from the SNOMED release alone by `python -m ladder.keywords --build`; nothing in it reads CADEC or the gold annotations. Findings and disorders only — resolving through Registry.resolve searched every description in the release, which is what answered |California chicken (organism)| for a rectal bleed and let |Gaseous substance| outrank the right concept for 'gas'. There is NO fallback to the registry when a name is absent: an unresolved name is unresolved, and falling back would reinstate the search this replaces and hide which of the two answered. The registry is still loaded — rung 1 needs exists/is_active/finding_status/terms over the WHOLE release, and S2's shortlist is retrieved from it."
    },
    "1": {
      "mode": "observe",
      "mode_note": "Rung 1 judges without routing. Its verdicts are recorded, counted and reported, but the record's zone is untouched, so rungs 3-6 see the full unfiltered set and each rung's marginal contribution stays attributable to that rung. Rung 2, which runs last, is where a rung 1 verdict is allowed to cost coverage. Set 'gate' to make rung 1 filter, as the plan's flow implies.",
      "reject_inactive": false,
      "check_negation": true,
      "negation_window": 6,
      "finding_check": true,
      "finding_scope": "reaction",
      "lexical_mode": "exact",
      "lexical_mode_note": "exact chosen over contained by measurement: contained accepts 19.0% of planted near-miss codes vs 0.1% for exact, buying 11.4pt of ACCEPT-lane coverage. See docs/decisions.md and out/rung1_detection*.json.",
      "meddra_check": "flag",
      "label_check": "flag",
      "label_check_note": "off | flag | reject. Rung 0's sct_label against the vocabulary's own words for the code it chose. Catches what exists() cannot: 82249009 is real and active and means |California chicken (organism)|, which granite4:micro-h emitted for 'extreme rectal bleed'. FLAG, not reject — 'rectal bleeding' against |Rectal hemorrhage| is one concept in two wordings, and this check's false-rejection floor has not been measured. Same posture as meddra_check and negation_action, and for the same reason.",
      "outdated_check": "flag",
      "outdated_check_note": "off | flag. A retired code SNOMED has since replaced is out of date, not invented: the model named a real concept and lacked the newer release. Rung 1 records sct_outdated and sct_replacement and NEVER rejects on them — same posture as meddra_check, negation_action and label_check. The successor comes from the association refset (SAME AS / REPLACED BY only; POSSIBLY EQUIVALENT TO and WAS A are excluded on purpose) and is scored as its own outcome in ladder/score.py, never folded into 'correct'. Measured 2026-08-24: only 27.3% of CADEC's 407 retired gold reaction mentions have such a successor at all."
    },
    "3": {
      "k": 3,
      "temperature": 0.7
    },
    "5": {
      "tau": 0.0,
      "abstain_zones": [
        "BAND"
      ],
      "abstain_on_reject": true,
      "tau_tuned_on": "dev",
      "tau_note": "tau is swept on dev only and written here BEFORE the first test run. 0.0 means the confidence gate is off until a real rung-0 confidence distribution exists to calibrate against."
    },
    "6": {
      "mode": "simulated",
      "minutes_per_record": 2.0
    }
  },
  "output": {
    "dir": "out"
  },
  "scorer": "ladder.score:reaction_sct_strict",
  "scorer_note": "Added 2026-08-23; until then the accuracy columns were written empty rather than guessed. Gold is keyed by SPAN, never by position: under index-based comparison a perfect extraction listed in another order scores 0.216. span_match is 'exact' for the headline; 'overlap' exists because rung 3 keys on exact spans and its resamples never align."
}
