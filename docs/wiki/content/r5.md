# Rung 5 · voting {{todo|NOT STARTED}}

`ladder/rungs/r5.py` — not written. **k calls per record.**

## Role

- Sample the extractor k times, take the majority.
- **Majority on the normalised code, never on the string.** `cramping` and `Muscle cramp` are the same answer; a string vote would split them and report disagreement that does not exist.

## Settings

`manifest.rungs.5`

| Setting | Value |
|---|---|
| `k` | 3 |
| `temperature` | 0.7 |

- Temperature is deliberately **not** 0 here — identical samples cannot vote. This is the one place the project departs from temp 0, and it is recorded in the manifest for that reason.

## Cost

- k calls per record. The most expensive rung in tokens, and the one whose marginal value the ladder is most likely to question.

## Related

- [[r4]] · [[rungs]] · [[ledger]] · [[manifest]]
