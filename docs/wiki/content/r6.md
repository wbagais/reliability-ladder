# Rung 6 · human-in-the-loop {{todo|NOT STARTED}}

**Cost measured in human minutes, never converted to currency.**

## Role

- A person settles what the ladder could not.
- Runs last. Everything still open after [[r2]] is what reaches a human.

## Settings

`manifest.rungs.6`

| Setting | Value |
|---|---|
| `mode` | `simulated` |
| `minutes_per_record` | 2.0 |

- `simulated` costs the queue without a person actually working it. A timed mode would measure real minutes.

## It is a rung, not a prompt

- "Tell the model to escalate when unsure" is [[r2]], not rung 6. Rung 6 is a person.

## What a person is shown

- The record, and `checks["withheld"]` — what the system was going to say before [[r2]] withdrew it.
- Abstention is a withdrawal, never a deletion, precisely so this rung has something to show.

## Cost measure

- `human_minutes` in the [[ledger]], surfaced as `reviews_per_100`.
- Never fused with tokens. The question the project exists to ask is *would you rather spend tokens or human attention* — fusing them deletes the question.

## Related

- [[r2]] · [[rungs]] · [[ledger]]
